{
  "name": "states-ranked",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d dist"
  },
  "devDependencies": {
    "astro": "^1.0.0-rc.2",
    "gh-pages": "^4.0.0"
  }
}
